import React from 'react';

const HomePage = ({text}) => {
    return (
        <div>
            <h1>{text}</h1>
            {/* Add your content here */}
        </div>
    );
};

export default HomePage;
